# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local config / state (never publish a user's real paths or secrets)
obsidian-pilot.config.json
*.pilot-bak
*.log
*.lock
*.last

# Demo build artifacts (the rendered hero.png / demo.gif are committed; frames are not)
docs/frames/

# OS cruft
.DS_Store
Thumbs.db
desktop.ini
